// Online C compiler to run C program online
#include <stdio.h>
int main() {
    int n = 4287;
    int sum=0;
    while(n>0){
        sum += n%10;
        n=n/10;
    }
    printf("sum of digits of given number is %d",sum);
    return 0;
}
